# 🦀 Hello World em Rust

Este é um projeto **Hello World** em **Rust**, criado para demonstrar a estrutura mínima de um programa na linguagem e servir como primeiro contato para iniciantes.

---

## 📌 Objetivo

* Entender a estrutura básica de um programa em Rust
* Aprender o papel da função `main`
* Utilizar a macro `println!` para exibir mensagens no terminal

---

## 📄 Código Fonte

```rust
fn main() {
    println!("Hello, world!");
}
```

---

## 🧠 Explicação do Código

### 🔹 Função `main`

* É o ponto de entrada do programa
* Todo programa em Rust começa a execução por essa função

### 🔹 Macro `println!`

* Utilizada para imprimir texto no terminal
* O ponto de exclamação (`!`) indica que se trata de uma **macro**, não de uma função comum

---

## ▶️ Como Executar

1. Verifique se o Rust está instalado:

   ```bash
   rustc --version
   ```

2. Crie um novo projeto:

   ```bash
   cargo new hello_world
   ```

3. Substitua o conteúdo do arquivo `src/main.rs` pelo código acima

4. Execute o programa:

   ```bash
   cargo run
   ```

---

## 🖨️ Saída Esperada

```
Hello, world!
```

---

## 🚀 Próximos Passos

* Modificar a mensagem exibida
* Criar mais chamadas de `println!`
* Aprender sobre variáveis e tipos em Rust

---

## 📚 Conclusão

Este exemplo representa o primeiro passo no aprendizado de **Rust**, mostrando como é simples iniciar um programa e interagir com o terminal.

Feliz
